# Изменения в версии 3.1

Этот документ содержит описание всех изменений, исправляющих критические проблемы безопасности и стабильности, выявленные в ходе аудита.

---

## 🔴 Критические исправления

### 1. Захардкоженный CONTAINER_TAG (БЕЗОПАСНОСТЬ)
**Файлы:** `sm-daemon.js`, `sm-recall.js`, `sm-sync-files.js`

**Проблема:** `CONTAINER_TAG: 'your-name'` был захардкожен во всех скриптах. Если пользователь забыл изменить это значение, все данные попадали в один контейнер, что приводило к смешиванию данных между пользователями.

**Решение:**
- Перемещён `CONTAINER_TAG` в переменную окружения `SM_CONTAINER_TAG`
- Добавлена валидация, которая отклоняет placeholder-значения (`your-name`, `test`, `example` и т.д.)
- Минимальная длина — 2 символа
- Резервное значение `'default'` используется только с предупреждением, если переменная не задана
- Скрипт завершается с ошибкой, если обнаружено placeholder-значение

**Использование:**
```bash
export SM_CONTAINER_TAG=my-unique-id
```

---

### 2. Отсутствие HTTP-таймаутов (СТАБИЛЬНОСТЬ)
**Файлы:** `sm-daemon.js`, `sm-recall.js`, `sm-sync-files.js`

**Проблема:** Supermemory SDK v4.11.1 не устанавливает таймауты по умолчанию. Если API зависает — демон зависает навсегда.

**Решение:**
- Добавлен `AbortController` с таймаутом 30 секунд (настраивается через `SM_API_TIMEOUT_MS`)
- Таймаут применяется ко всем API-вызовам через обёртку `apiCallWithRetry()`
- Корректная очистка ресурсов таймаута через `clearTimeout()`

---

### 3. Отсутствие логики повторных попыток (НАДЁЖНОСТЬ)
**Файлы:** `sm-daemon.js`, `sm-recall.js`, `sm-sync-files.js`

**Проблема:** Временные сетевые ошибки (502, таймаут) прерывали всю операцию синхронизации.

**Решение:**
- Реализована функция `apiCallWithRetry()`:
  - Настраиваемое количество попыток (по умолчанию: 3, через `SM_API_RETRY_ATTEMPTS`)
  - Экспоненциальная задержка с джиттером: 1с, 2с, 4с + случайные 0-1с
  - Умный retry: не повторяет клиентские ошибки 4xx (кроме 429 rate limit)
  - Не повторяет при отмене пользователем

---

### 4. Race Condition при graceful shutdown (СТАБИЛЬНОСТЬ)
**Файл:** `sm-daemon.js`

**Проблема:** Исходный код:
```javascript
const shutdown = () => { 
  log('🛑 Shutdown'); 
  saveState(loadState()); // ← перезагружает состояние из файла, теряя несохранённые изменения
  process.exit(0); 
};
```

**Решение:**
- Хранение `currentState` в переменной замыкания
- Использование `currentState` напрямую в обработчике shutdown вместо перезагрузки
- Гарантия сохранения всех несохранённых изменений при завершении

---

### 5. PID-файл не очищается при аварийном завершении (ЭКСПЛУАТАЦИЯ)
**Файл:** `sm-control.sh`

**Проблема:** Если демон был убит через SIGKILL или упал (OOM), PID-файл оставался. При следующем `start` возникала ошибка "Already running".

**Решение:**
- Добавлена функция `is_daemon_running()`, которая проверяет:
  - Существует ли процесс с данным PID (`kill -0`)
  - Содержит ли `cmdline` процесса "sm-daemon.js" (защита от переиспользования PID)
- Добавлена функция `cleanup_stale_pid()` для удаления осиротевших PID-файлов
- Вспомогательная функция `get_valid_pid()` объединяет валидацию и очистку
- Применяется ко всем командам: `start`, `stop`, `status`, `check`

---

### 6. API-ключ может утекать в логи (БЕЗОПАСНОСТЬ)
**Файлы:** `sm-daemon.js`, `sm-recall.js`, `sm-sync-files.js`

**Проблема:** Сообщения об ошибках от SDK могли содержать API-ключи в URL или заголовках, которые попадали в логи напрямую.

**Решение:**
- Реализована функция `scrubSensitiveData()` с regex-паттернами для:
  - Ключей Supermemory API (`sm_...`)
  - Общих паттернов API-ключей
  - Заголовков Authorization
  - Bearer-токенов
- Применяется scrubbing ко всему выводу в логи/ошибки
- Формат: `[REDACTED_API_KEY]`, `[REDACTED]`

---

## 🟡 Дополнительные улучшения

### Логирование ошибок парсинга JSON
**Файл:** `sm-daemon.js`

Ранее ошибки парсинга JSON в файлах сессий игнорировались без логирования. Теперь логируются с указанием номера строки для отладки.

### Улучшенная обработка ошибок в sm-sync-files.js
**Файл:** `sm-sync-files.js`

Ранее одна неудачная загрузка прерывала всю синхронизацию. Теперь каждый файл обёрнут в try-catch, ошибки логируются, но синхронизация продолжается для остальных файлов.

### Команда проверки без вывода
**Файл:** `sm-control.sh`

Добавлена команда `check` для heartbeat-скриптов:
```bash
bash sm-control.sh check  # возвращает 0 если запущен, 1 если нет (без вывода)
```

---

## 📁 Новые файлы

### `.env.example`
Полный пример конфигурации с описанием всех переменных окружения:
- Обязательная: `SM_CONTAINER_TAG`
- Опциональные пути, размеры батчей, интервалы
- Конфигурация таймаутов и повторных попыток API

---

## 📄 Изменённые файлы

| Файл | Изменения |
|------|-----------|
| `sm-daemon.js` | +350 строк: env-конфиг, валидация, таймаут, retry, scrubbing, исправление race condition |
| `sm-recall.js` | +200 строк: env-конфиг, валидация, таймаут, retry, scrubbing |
| `sm-sync-files.js` | +200 строк: env-конфиг, валидация, таймаут, retry, scrubbing, обработка ошибок по файлам |
| `sm-control.sh` | +120 строк: обнаружение stale PID, функции валидации, команда check |
| `README.md` | Полная переработка: документация env-переменных, гайд миграции, фичи v3.1 |

---

## 🔒 Сводка улучшений безопасности

| Проблема | Было | Стало |
|----------|------|-------|
| Изоляция данных | Захардкоженный `your-name` | Валидируемая env-переменная |
| API-ключ в логах | Прямое логирование | Regex-очистка |
| Управление процессами | Простая проверка PID | Проверка по cmdline |

## 🚀 Сводка улучшений стабильности

| Проблема | Было | Стало |
|----------|------|-------|
| Зависание API | Навсегда | 30с таймаут |
| Сетевые ошибки | Немедленный fail | 3 попытки с backoff |
| Shutdown | Race condition | Корректное сохранение состояния |
| Восстановление после краша | Ручная очистка | Автоочистка stale PID |

---

## Руководство по миграции с v3.0

1. Установить обязательную переменную окружения:
   ```bash
   export SM_CONTAINER_TAG=your-unique-id
   ```

2. (Опционально) Ознакомиться с другими настройками в `.env.example`

3. Все остальные настройки имеют разумные значения по умолчанию — обратно совместимы.

4. Запускать скрипты как обычно.

---

*Аудит завершён: 2026-02-15*  
*Исправления реализованы: v3.1*
